# CSS Quest — игра для тренировки основ CSS

Прототип: Vite + React. Быстро стартовать и масштабировать.

Запуск:

```bash
npm install
npm run dev
```

Особенности MVP:
- Выбор уровня
- Редактор CSS + превью
- Кнопки Run и Check (проверка уровня)
- Сохранение прогресса в `localStorage`
